# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.local
.env.production
.env.*.local
.env.windows
!.env.production.example
!.env.windows.example
.wmcdss_ai_settings.json
backend/.wmcdss_ai_settings.json
.claude/
.mcp.json
CLAUDE.md
.coderabbit.yaml.bak-*
*.pem
*.key

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp

# Agent tool state (local session/worktree data, not project artifacts)
.omo/
.opencode/

# Build / dist
dist/
build/
*.egg

# Logs / runtime
*.log
*.pid
state.json

# Coverage outputs
.coverage
htmlcov/
coverage/
frontend/vite-app/coverage/

# Docker / DB volumes (mounted via named volumes; nothing local)
pgdata/

# Pre-project proposal documents (kept locally for reference, not source-of-truth).
# Live design docs go under docs/. The 8.5MB sample HTML in particular must not
# bloat the repo — extract the relevant UI snippets into docs/ if reused.
/現場気象海象 自動集計・施工判断支援システム.md
/現場気象海象 自動集計・施工判断支援システム.txt
/現場気象海象 自動集計・施工判断支援システム提案資料.html
/現場気象海象 自動集計・施工判断支援システム提案資料サンプルWebUI.html
