{
  "name": "wmcdss-frontend",
  "private": true,
  "version": "0.1.0-phase0",
  "type": "module",
  "description": "WMCDSS dashboard — Vite migration scaffold (Phase 0). Existing Babel Standalone build at ../index.html remains the production entry until Phase 1 ports the .jsx modules.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^3.0.0",
    "vite": "^6.0.7",
    "vitest": "^3.0.0"
  }
}
